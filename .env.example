
# Application secret key (change to a secure random value)
SECRET_KEY=replace-me

# Admin API key used to access admin endpoints (set a long random value)
ADMIN_API_KEY=replace-with-a-secret-key

# SMTP (optional): used to send notifications when contact/signup received
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=smtp-user@example.com
SMTP_PASS=supersecret
ADMIN_EMAIL=you@yourdomain.com

# Example: copy to .env and edit values
# cp .env.example .env && nano .env
